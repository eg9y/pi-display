/* Duplicate this file as secrets.h and fill in your real credentials.
   secrets.h is listed in .gitignore so it's never committed.
*/
#ifndef SECRETS_H
#define SECRETS_H

const char* WIFI_SSID     = "YOUR_SSID_HERE";
const char* WIFI_PASSWORD = "YOUR_PASSWORD_HERE";

#endif
